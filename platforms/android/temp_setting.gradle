android {
            defaultConfig {
                applicationId = "ca.forces.hrciv.hrgo"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }