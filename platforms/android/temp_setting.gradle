android {
            defaultConfig {
                applicationId = "org.nativescript.HRGO"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }